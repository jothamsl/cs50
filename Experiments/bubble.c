#include <stdio.h>
#include <cs50.h>
#include <stdlib.h>

void bubble(string n);

int main(void)
{
    string list;
    do
    {
        list = get_string("Values: ");
    }
    while(list == (NULL || 0)); {
        bubble(list);
    }
}

void bubble(string n){
    int c;
    int list[]
    printf("%c\n", n);
}